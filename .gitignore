data/
node_modules/
.next/
.env*.local

# Next.js / TypeScript build artifacts
*.tsbuildinfo
next-env.d.ts

# Machine-generated by `next dev`; addressed to coding agents, not to a
# GoldBank project reader, and rewritten every dev run (spurious dirty diff
# for every cloner). Kept on disk, never committed.
AGENTS.md
CLAUDE.md
